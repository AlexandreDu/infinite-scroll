import styled from 'styled-components';

export const User = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  padding: 0.25rem;
  gap: 0.25rem;
`;

export const UserTitle = styled.span`
  font-weight: bold;
`;
